import ReactTestUtils from 'react-dom/test-utils'

describe('Navbar', () => {
  it('passing test', () => {
    expect(true).toBeTruthy();
  })
  it('failing test', () => {
    expect(false).toBeFalsy();
  })
})